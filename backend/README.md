# Backend

FastAPI backend for transcript ingestion, preprocessing, LangGraph question generation, persistence, and API access.

Zoom integration files are scaffolded only. Zoom logic will be implemented in a later module.

